ABORDAGEM:
Será necessário um tabuleiro NxN em que N é especificado
o número de peças vai de 2..N^2.

dominio(House, 1, N).

Cada casa terá uma coordenada (x, y).

Haverá varios tamanhos possíveis hardcoded, mas o user não vai
especificar NADA, não há menus, é tudo automático, gera-se um problema,
gera-se a solução e apresenta-se o resultado final.

Variaveis Decisão:
Lengths - só pode haver 2 lengths, distintas.
Houses - pode haver várias houses.

Usar predicado nvalue - dado lista de variaves de domínio, 
devolve quantos valores diferentes tem.

Será para aplicar à lista lengths, para só ter dois lengths diferentes.

Lengths = [L1, L2];
Houses = [H1, H2, H3 ... ];

Agrupar casas e calcular distâncias ...
distance = (xi - x)^2 + (yi - y)^2 (não usar square root porque os 
domínios têm que ser inteiros)

L1 #=/= L2


Falta tirar as simetrias